/// Store classification (design_spendlens.md §3; ARB keys
/// `storeType{Supermarket,Market,Pharmacy,Cafe,Store,Other}` in
/// `app_en.arb`).
enum EStoreType {
  supermarket,
  market,
  pharmacy,
  cafe,
  store,
  other,
}
